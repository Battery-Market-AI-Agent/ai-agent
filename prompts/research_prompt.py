"""T2, T3 공통: 기업 조사 프롬프트 (회사명만 변수)"""

# 6개 조사 카테고리 (dummy_data 구조와 일치)
RESEARCH_CATEGORIES = [
    "사업 포트폴리오",
    "기술 경쟁력",
    "재무 현황",
    "공급망/생산",
    "시장 지위",
    "리스크",
]

# LLM이 12개 Tavily 검색 쿼리를 생성하도록 지시하는 프롬프트
# - 입력 변수: {company}
# - 출력: JSON {"사업 포트폴리오": {"positive": ["쿼리1"], "negative": ["쿼리1"]}, ...}
QUERY_GENERATION_PROMPT = """\
당신은 배터리 산업 리서치 전문가입니다.
아래 프레임에 따라 {company}에 대한 Tavily 웹 검색 쿼리를 생성하세요.

[업종 컨텍스트]
- 산업: 배터리 (전기차용 배터리, ESS, HEV용 배터리)
- 주요 키워드: 전기차 캐즘, HEV 피벗, ESS 성장, 양극재, 가동률, AMPC 보조금, 전고체 배터리

[카테고리 정의 및 세부 힌트]
- 사업 포트폴리오: 제품 라인업, ESS·HEV·EV 다각화, 신사업 진출, 수주 현황
- 기술 경쟁력: 에너지밀도, 충전속도, 전고체·LFP·나트륨이온 기술, 특허
- 재무 현황: 매출, 영업이익/손실, 부채, AMPC 보조금 수혜, 투자 규모
- 공급망/생산: 공장 가동률, 원자재 조달, 합작법인, 생산능력 확장
- 시장 지위: 글로벌 점유율, 주요 고객사, 수주 경쟁, 지역별 포지션
- 리스크: 중국산 경쟁, 원자재 가격, 고객 이탈, 무역 규제, 지정학

[쿼리 작성 규칙]
- 각 카테고리당 긍정 방향 쿼리 1개 + 부정 방향 쿼리 1개 (총 12개)
- 긍정: 성장·성과·강점 관점의 키워드
- 부정: 손실·리스크·약점 관점의 키워드
- Tavily 웹검색에 최적화된 간결한 키워드 조합 (문장형 금지)
- 2025~2026년 최신 데이터를 타겟으로 연도 포함

[출력 규칙]
- 반드시 JSON만 출력 (설명, 마크다운 코드블록 금지)

{{
  "사업 포트폴리오": {{"positive": "긍정 쿼리", "negative": "부정 쿼리"}},
  "기술 경쟁력":    {{"positive": "긍정 쿼리", "negative": "부정 쿼리"}},
  "재무 현황":      {{"positive": "긍정 쿼리", "negative": "부정 쿼리"}},
  "공급망/생산":    {{"positive": "긍정 쿼리", "negative": "부정 쿼리"}},
  "시장 지위":      {{"positive": "긍정 쿼리", "negative": "부정 쿼리"}},
  "리스크":         {{"positive": "긍정 쿼리", "negative": "부정 쿼리"}}
}}

분석 대상 회사: {company}
"""

# 검색 결과가 해당 카테고리를 충분히 커버하는지 LLM이 판단
# - 입력 변수: {company}, {category}, {category_definition}, {query}, {results}
# - 출력: {"grade": "sufficient" | "insufficient", "reason": "한 문장"}
RESULT_EVALUATOR_PROMPT = """\
아래 검색 결과가 분석에 충분한지 평가하세요.

- 분석 회사: {company}
- 조사 카테고리: {category} ({category_definition})
- 검색 쿼리: {query}

[검색 결과]
{results}

[판단 기준 - 하나라도 미충족이면 insufficient]
1. {company}에 대한 직접적인 정보가 포함되어 있는가?
2. 2025년 이후 자료인가? (2025, 2026, 2027 등 모두 해당, 날짜 불명확 시 내용으로 추정)
3. {category} 항목과 관련된 내용인가?

반드시 JSON만 출력 (설명, 마크다운 코드블록 금지):
{{"grade": "sufficient", "reason": "한 문장 이유"}}
"""

# insufficient 판정 시 쿼리를 재작성하도록 지시
# - 입력 변수: {company}, {category}, {sentiment}, {original_query}, {reason}
# - 출력: 쿼리 문자열 1개 (따옴표·설명 없이)
QUERY_REFINEMENT_PROMPT = """\
아래 검색 쿼리가 충분한 결과를 반환하지 못했습니다.
더 구체적인 쿼리 1개로 재작성하세요.

- 분석 회사: {company}
- 조사 카테고리: {category}
- 쿼리 방향: {sentiment} (positive=긍정·성과·강점 / negative=리스크·손실·약점)
- 원래 쿼리: {original_query}
- 실패 이유: {reason}

[재작성 규칙]
- 더 구체적인 키워드 사용, 연도 명시
- {sentiment} 방향 키워드 유지
- Tavily 웹검색용 키워드 조합 (문장형 금지)

재작성된 쿼리만 출력 (따옴표·설명 없이):
"""

# 수집된 raw 전체 → 카테고리별 요약 문자열 생성
# - 입력 변수: {company}, {raw}
# - 출력: 6개 카테고리별 2~3문장 요약
RESEARCH_SUMMARY_PROMPT = """\
아래는 {company}에 대한 웹 검색 원문입니다.
6개 카테고리별로 핵심 내용을 요약하세요.

[요약 원칙]
- 검색 원문에 근거한 내용만 포함 (추측·일반론 금지)
- 각 카테고리 2~3문장
- 출처가 명확한 수치·사실 위주로 작성

[검색 원문]
{raw}

[출력 형식]
사업 포트폴리오: ...
기술 경쟁력: ...
재무 현황: ...
공급망/생산: ...
시장 지위: ...
리스크: ...
"""
