"""T2, T3 공통: 기업 조사 프롬프트 (회사명만 변수)"""

# SWOT 4개 카테고리 프레임 (구조 고정, LLM에 주입)
SWOT_CATEGORIES = {
    "S": "기술·제품 강점 + 포트폴리오 전략",
    "W": "재무·운영 약점",
    "O": "시장 기회 + 전략적 대응",
    "T": "경쟁·공급망 위협",
}

# LLM이 8개 Tavily 검색 쿼리를 생성하도록 지시하는 프롬프트
# - 입력 변수: {company}
# - 출력: JSON {"S": {"ko": "...", "en": "..."}, "W": ..., "O": ..., "T": ...}
QUERY_GENERATION_PROMPT = """\
당신은 배터리 산업 리서치 전문가입니다.
아래 프레임에 따라 {company}에 대한 Tavily 웹 검색 쿼리를 생성하세요.

[업종 컨텍스트]
- 산업: 배터리 (전기차용 배터리, ESS, HEV용 배터리)
- 주요 키워드: 전기차 캐즘, HEV 피벗, ESS 성장, 양극재, 가동률, AMPC 보조금, 전고체 배터리

[SWOT 항목 정의 및 세부 힌트]
- S (강점): 기술경쟁력(에너지밀도·충전속도·전고체), 제품 포트폴리오(ESS·HEV 다각화), 주요 고객사·수주 현황, 생산 효율
- W (약점): 영업손실·부채·재무 악화, 가동률 하락, 품질 이슈·리콜, 특정 고객 의존도
- O (기회): ESS 시장 고성장, HEV 수요 급증, 북미 AMPC 보조금 수혜, 신흥시장 진출
- T (위협): 중국산 저가 배터리 경쟁, 리튬·코발트 원자재 가격 변동, 주요 고객사 내재화, 무역 규제

[쿼리 작성 규칙]
- S/O: 성장·성과·기회 관점의 긍정 방향 키워드로 구성
- W/T: 손실·리스크·위협 관점의 부정 방향 키워드로 구성
- 각 항목당 한국어 쿼리 2개 + 영어 쿼리 2개 (총 16개)
- 같은 항목 내 2개 쿼리는 반드시 서로 다른 서브토픽을 커버
  예) S의 ko1: 기술·경쟁력 초점 / ko2: 포트폴리오·전략 초점
- Tavily 웹검색에 최적화된 간결한 키워드 조합 (문장형 금지)
- 2025~2026년 최신 데이터를 타겟으로 연도 포함

[출력 규칙]
- 반드시 JSON만 출력 (설명, 마크다운 코드블록 금지)

{{
  "S": {{"ko": ["한국어 쿼리1", "한국어 쿼리2"], "en": ["English query1", "English query2"]}},
  "W": {{"ko": ["한국어 쿼리1", "한국어 쿼리2"], "en": ["English query1", "English query2"]}},
  "O": {{"ko": ["한국어 쿼리1", "한국어 쿼리2"], "en": ["English query1", "English query2"]}},
  "T": {{"ko": ["한국어 쿼리1", "한국어 쿼리2"], "en": ["English query1", "English query2"]}}
}}

분석 대상 회사: {company}
"""

# 검색 결과가 해당 SWOT 항목을 충분히 커버하는지 LLM이 판단
# - 입력 변수: {company}, {swot_item}, {swot_definition}, {query}, {results}
# - 출력: {"grade": "sufficient" | "insufficient", "reason": "한 문장"}
RESULT_EVALUATOR_PROMPT = """\
아래 검색 결과가 분석에 충분한지 평가하세요.

- 분석 회사: {company}
- SWOT 항목: {swot_item} ({swot_definition})
- 검색 쿼리: {query}

[검색 결과]
{results}

[판단 기준 - 하나라도 미충족이면 insufficient]
1. {company}에 대한 직접적인 정보가 포함되어 있는가?
2. 2025년 이후 자료인가? (날짜 불명확 시 내용으로 추정)
3. {swot_item} 항목({swot_definition})과 관련된 내용인가?

반드시 JSON만 출력 (설명, 마크다운 코드블록 금지):
{{"grade": "sufficient", "reason": "한 문장 이유"}}
"""

# insufficient 판정 시 쿼리를 재작성하도록 지시
# - 입력 변수: {company}, {swot_item}, {original_query}, {reason}
# - 출력: 쿼리 문자열 1개 (따옴표·설명 없이)
QUERY_REFINEMENT_PROMPT = """\
아래 검색 쿼리가 충분한 결과를 반환하지 못했습니다.
더 구체적인 쿼리 1개로 재작성하세요.

- 분석 회사: {company}
- SWOT 항목: {swot_item}
- 원래 쿼리: {original_query}
- 실패 이유: {reason}

[재작성 규칙]
- 원래 언어(한국어/영어) 유지
- 더 구체적인 키워드 사용, 연도 명시
- S/O는 긍정 방향, W/T는 부정 방향 키워드 유지
- Tavily 웹검색용 키워드 조합 (문장형 금지)

재작성된 쿼리만 출력 (따옴표·설명 없이):
"""

# 수집된 raw 전체 → SWOT별 요약 문자열 생성
# - 입력 변수: {company}, {raw}
# - 출력: S/W/O/T 각 항목 2~3문장 요약
RESEARCH_SUMMARY_PROMPT = """\
아래는 {company}에 대한 웹 검색 원문입니다.
SWOT 4개 항목별로 핵심 내용을 요약하세요.

[요약 원칙]
- 검색 원문에 근거한 내용만 포함 (추측·일반론 금지)
- 각 항목 2~3문장
- 출처가 명확한 수치·사실 위주로 작성

[검색 원문]
{raw}

[출력 형식]
S (강점): ...
W (약점): ...
O (기회): ...
T (위협): ...
"""
